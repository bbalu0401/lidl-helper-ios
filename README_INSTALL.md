# Lidl Helper iOS App (Dark Mode)

## Setup
1. npm install
2. npm run dev  — development
3. npm run build — production build
4. npx cap sync ios — sync Capacitor
5. Run in Appflow or AltStore as iOS app

Supabase URL + anon key already configured.
